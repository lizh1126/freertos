# VS Code
.vscode/

# ==========================================
# 1. STM32CubeMX 临时文件
# ==========================================
# .mxproject 是 CubeMX 每次生成代码时顺带生成的工程状态记录文件，
# 里面包含绝对路径，且每次生成都会变，不需要加入版本控制。
.mxproject
*.bak
*.log

# ==========================================
# 2. Keil MDK-ARM 个人配置文件 (必须屏蔽)
# ==========================================
# 这些文件记录了你电脑上窗口的位置、断点打在哪、调试器的序列号等。
# 如果不屏蔽，每次你打个断点 Git 都会提示文件已修改。
*.uvoptx
*.uvguix.*
*.uvgui.*
*.Administrator
*.scvd
JLinkLog.txt
DebugConfig/

# ==========================================
# 3. 编译生成的中间文件和输出文件 (必须屏蔽)
# ==========================================
# MDK 编译时会生成大量的中间文件和固件，这些都不应该提交
*.o
*.d
*.crf
*.htm
*.dep
*.lst
*.map
*.lnp
*.axf
*.hex
*.bin
*.elf
*.build_log.htm
*.iex

# MDK 默认会在 MDK-ARM 目录下生成一个和工程同名的文件夹来存放编译产物。
# 比如你的工程叫 stm32h743_freertos，它就会生成 MDK-ARM/stm32h743_freertos/
# 由于上面已经按后缀名（*.o, *.axf等）屏蔽了，通常不需要再单独写文件夹，
# 但如果你想彻底一点，可以把下面这行前面的 # 去掉，并换成你的工程名：
# MDK-ARM/stm32h743_freertos/

# ==========================================
# 4. 操作系统临时文件
# ==========================================
.DS_Store
Thumbs.db

# ==========================================
# 5. Segger Ozone / J-Link 调试相关文件
# ==========================================
# 屏蔽 Ozone 的跟踪/录制数据文件（这些文件通常是 MB 甚至 GB 级别的二进制数据）
*.jtrace
*.jtrace.*

# 屏蔽 Ozone 的用户个人布局文件（不同版本 Ozone 行为不同，如果有的话屏蔽掉）
*.jdebug.user

# 屏蔽 SystemView 等工具可能产生的实时系统事件记录数据
*.svdat